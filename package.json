{
  "name": "go-links-clone",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server",
    "infra"
  ],
  "scripts": {
    "build": "npm run build --workspace client && npm run build --workspace server && npm run build --workspace infra",
    "dev": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "lint": "npm run lint --workspace server && npm run lint --workspace client && npm run lint --workspace infra",
    "deploy:aws": "npm run build --workspace client && npm run cdk:deploy:app --workspace infra",
    "apply:self-hosting": "bash scripts/apply-self-hosting.sh",
    "sync:github-vars": "bash scripts/sync-github-vars-from-terraform.sh",
    "doctor:self-hosting": "bash scripts/doctor-self-hosting.sh"
  }
}
